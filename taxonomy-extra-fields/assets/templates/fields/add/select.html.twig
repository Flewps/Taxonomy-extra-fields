<div class="form-field">
	<label for="term_meta[{{ name }}]">{{ label }}</label>
	<select name="term_meta[{{ name }}]" class="{{ class }}" >
		<option>{{ placeholder }}</option>
	</select>
	{% if not description is empty %}
		<p class="description">{{ description }}</p>
	{% endif %}
</div>