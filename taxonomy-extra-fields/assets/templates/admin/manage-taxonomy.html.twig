{% extends 'admin/base.html.twig' %}

{% block title %}{{ title }}{% endblock %}
 
{% block content %}
    
    <div>
    	<form name="defaults" id="defaults">
    		<input type="hidden" name="position" value="2" />
    		<input type="hidden" name="taxonomy" value="{{ taxonomy.name }}" />
    	</form>	
    	<a href="{{ url_new }}" class="button button-primary add-new-field">{{ translations.addnew }}</a>
    </div>
    
    
    {{ table.display() }}

	<div>
		<a href="{{ url_new }}" class="button button-primary add-new-field">{{ translations.addnew }}</a>
	</div>

{% endblock %}